## Qué cambia

## Checklist

- [ ] Si toqué contenido de la wiki o `data/cifras.json`, actualicé
      `fuente`, `verificadoPor` y `fechaVerificacion`.
- [ ] Si toqué `donar-dinero` o `donar-especie`, entiendo que este PR
      necesita revisión humana antes de mergear (riesgo de canales falsos).
- [ ] `npm run build` pasa localmente sin errores.
